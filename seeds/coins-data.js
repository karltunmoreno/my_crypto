const sequelize = require('../config/connection');
const { Coins, Post } = require('../models');
const rawCoin = require ("../index.js");



const feedData = [
    rank: ,
    name: ,
    price:  

]